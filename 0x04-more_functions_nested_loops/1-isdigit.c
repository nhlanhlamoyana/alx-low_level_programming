#include "main.h"
/** 
 * _isdigit - checks for a digit
 * @c: checks function parameter
 * Return: 1 when successful
 */

int _isdigit(int c);
{
if (c >= 48 && c <= 57)
	return (1);
else
	return (0);

}
