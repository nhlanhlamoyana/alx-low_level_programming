#include "main.h"

/**
 * print_square - function that prints square using the '#' character
 * @size: size of the square
 * Return: void
 */

void print_square(int size)
{
if (size <= 0)
_putchar('\n');
else
{	int i, j;

for (i = 1 ; i <= size ; i++)
{	
for (j = 1 ; <= size ; j++)
_putchar('#');
_putchar_('\n');
}
}

}
